import React from 'react';
import {HeartTwoTone } from '@ant-design/icons';

const LikesButton = (props) => {
  return (
    <HeartTwoTone key={item._id} twoToneColor="#eb2f96"/>
  )
}

export default LikesButton;
